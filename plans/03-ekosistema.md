# План 03 — Экосистема: от трекера к системе прогресса

**Создан:** 2026-09-03. Запрос Григория после первого использования: главный экран «говно», хочет полноценную платформу — прогресс по силе, по мышцам, по замерам; прислал исторический дневник (май–сентябрь), хочет его импортировать.

## Что вскрыл фидбек

1. **Приложение слишком жёсткое.** Григорий тренируется не по программе Никиты — свой сплит, состав плавает, порядок в зале другой (занят тренажёр). Сессия не должна быть привязана к дню программы. → базовые правки уже в плане 01 (свободная навигация, «сам решаешь сколько подходов», общее время — сделано 2026-09-03).
2. **Нет кардио.** В дневнике куча бега и велотренажёра. Нужен тип записи «cardio».
3. **Нет прогресса.** Дневник = список, а не динамика. Нужны: сила по упражнению во времени, объём по мышцам, замеры тела.
4. **Главный экран пустой.** Должен показывать состояние: что на этой неделе, где рост, что дальше.
5. **Гифки.** Хочет видеть технику.

---

## Целевая картина

Личная система здоровья, тренировочный модуль — первый (см. `context/06`). Внутри модуля:

- **Свободная сессия** — добавляешь любое упражнение, любой порядок; план (A/B/C или другой) — подсказка, не рельсы.
- **Библиотека упражнений** — ~30 движений (Никита + исторические Григория) с мышцами, снарядом, гифкой, cue.
- **Кардио** — бег/велотренажёр: дистанция, время, сопротивление/горка.
- **Прогресс:**
  - по упражнению — рабочий вес / оценка 1ПМ / тоннаж во времени, график;
  - по мышце — недельный объём (подходов), динамика;
  - PR-ы — когда побил рекорд.
- **Тело** — вес, замеры (талия, грудь, бицепс, бедро…), фото, во времени.
- **Главный экран** — дашборд: неделя (объём по группам, тренировок), последние PR, следующая запланированная.

---

## Этапы

### Этап A — Свободная сессия (снять жёсткость)  ✅ 2026-09-04

- [x] A.1 Свободная навигация между упражнениями, авто-переход только на входе
- [x] A.2 Всегда доступен следующий подход, «сам решаешь сколько»
- [x] A.3 Общее время тренировки в шапке
- [x] A.4 **Добавить упражнение в сессию** на лету: лист «+ упражнение · порядок» → выбор из библиотеки → встаёт в конец. Сессия — новая таблица `sessionExercises` (снимок слотов дня на старте), не проекция программы
- [x] A.5 Убрать упражнение из сессии (`skipped`, программа не трогается). С записанными подходами не убирается — сначала удали подходы
- [x] A.6 Порядок упражнений — стрелками ↑↓ в том же листе. **Не drag:** на телефоне одной рукой стрелки надёжнее; вернуться к drag, если Григорий попросит

_Итог этапа A:_ сессия развязана от программы. Новая синк-таблица `sessionExercises` (Dexie v4 + Drizzle 0002 + `SYNC_TABLES`/`api/sync`), апгрейд v4 достраивает список для старых сессий из сохранённых слотов + упражнений их подходов. `today`/`diary`/`summary` не тронуты (крутятся от `setLogs`). Всё управление списком — в одном нижнем листе, главный экран сессии остался плотным.

### Этап B — Библиотека упражнений + гифки

- [x] B.1 Справочник расширен 26 → 36. `src/data/exercises-extra.ts` — 10 исторических (lat-pulldown, seated-cable-row, chest-supported-row, t-bar-row, pec-deck, barbell-bench, shoulder-press, hammer-curl, shrug, cable-crunch). `Glute` = `hip-abduction`, отдельного не заводил. `syncCatalog()` в бутстрапе добавляет отсутствующие в уже засиженные базы (только add-if-missing, свои правки не трогает)
- [x] B.2 `Exercise.muscle` → тип `MuscleGroup` (16 групп). Dexie v5 ремапит 4 старых значения (разгибатели спины→поясница, средняя ягодичная→ягодицы, приводящие бедра→приводящие, нижняя трапеция→трапеция). На сервере миграции нет — `muscle` там `text`, словарь на клиенте
- [ ] B.3 Источник гифок: проверить `github.com/hasaneyldrm/exercises-dataset` (лицензия, покрытие) или альтернативы из `context/04`. Нужно ~37, не 1300. **В работе** — `Exercise.gifUrl` уже показывается в библиотеке и на экране упражнения, если заполнено; сами файлы не подобраны
- [x] B.4 Экран упражнения: гифка (если есть) + cue + история («было»). Открывается тапом по названию в сессии/итоге/дневнике/библиотеке
- [x] B.5 Экран «Библиотека»: плитка на главном (2×2 с Дневник/Прогресс/Тело), список 37 упражнений, поиск по названию, фильтр-чипы по мышце. Не в зале — клавиатура тут не грабля

_Итог B.1–B.2, B.4–B.5:_ 37 упражнений, мышцы в контролируемом словаре, полноценная библиотека с поиском, экран упражнения везде доступен. B.3 (сами гифки) — единственное, что осталось из плана 03.

### Этап C — Импорт истории

- [x] C.1 Парсер `scripts/import-history.ts`. Dry-run пишет `scripts/_history-parsed.json` + отчёт; `--write` заливает в Neon (идемпотентно, id `imp-<ГГГГММДД>...`). Форматы: `вес kg reps, reps` (вес переносится), `reps*N`, `reps раз: вес, вес`, `вес, N подходов, в среднем R`, reps-first (`12 45 kg`), диапазон `50-55kg`→первый
- [x] C.2 Маппинг названий → слаги в скрипте (`NAME_MAP`). Недостающие упражнения — из `exercises-extra.ts` (B.1), `--write` их доупсертит в Neon
- [x] C.3 Кардио и freeform (`Разминка`, `Горы`, `хаотичная тренировка`) → `Session.note`. Схема: `Session.day` теперь nullable, `Session.source`, `Session.note` (Dexie v6, Drizzle 0003, прогнана в Neon)
- [x] C.4 **`--write` прогнан: 37 сессий, 506 подходов в Neon** (01.05–29.08.2026). Синкнутся на устройство Григория при заходе
- [x] C.5 `Session.source: "import"`, `day: null`. В дневнике — метка «· импорт», заметка (кардио/разминка) раскрывается

**Решения Григория (учтены):**
- `Glute` → то из отведения/сведения, что в этот день не названо явно (делает и то, и другое): 03.05 → `hip-adduction`, 17.05 → `hip-abduction`
- `Biceps` = новое упражнение `db-curl-seated` (сгибания гантелей сидя с доворотом, поясница прижата) — итого справочник 37
- `29.09` → `29.08` ✓
- строки без чисел → медиана веса/повторов/подходов по истории этого упражнения. Одна (`cable-crunch` 06.07) без базы — выпала

### Этап D — Кардио

- [ ] D.1 Модель: `cardioLog` (session_id, kind: run/bike, minutes, distanceKm, incline/resistance, note) или расширить `setLog` типом. Решить при проектировании
- [ ] D.2 В сессии: «+ кардио» → простая форма (время, дистанция)
- [ ] D.3 Импорт кардио из истории (C.3)

### Этап E — Прогресс  ✅ 2026-09-04

- [x] E.1 `src/progress/calc.ts` — `epley`, `topSetSeries` (лучший подход по 1ПМ в каждой сессии), `personalBests`, `prsInSession`, `recentWeightPRs`, `muscleVolume(weeks)`. Всё поверх setLog+session, ничего не хранится
- [x] E.2 Экран упражнения (`src/screens/exercise.tsx`): рекорд, график вес/1ПМ по датам (`ProgressChart` — инлайн SVG, тап по точке = подпись, PR-точки зелёные), список тренировок с этим упражнением. Открывается тапом по названию в сессии, итоге, дневнике
- [x] E.3 Экран «Прогресс» (`src/screens/progress.tsx`, плитка вместо «Программа»): бары объёма по мышцам 4/8/12 нед + список последних рекордов (тап → экран упражнения)
- [x] E.4 PR-детект: `prsInSession` — подход, побивший всю историю ДО этой сессии (по весу или 1ПМ). Метка «рекорд» в строке подхода и на упражнении в итоге

### Этап F — Тело  ✅ 2026-09-04

- [x] F.1 `BodyLog` — 13 полей явными колонками (вес + 12 обхватов, `bicepsL`/`bicepsR` раздельно), любое null. Dexie v7, Drizzle 0004 (прогнана). `BODY_FIELDS`/`BODY_LABELS` в схеме. База 5.12.2025 залита в Neon (`scripts/import-body.ts`)
- [x] F.2 Экран «Тело» (`src/screens/body.tsx`, плитка ожила): таблица последних значений + дельта к прошлому замеру (нейтральная, не красим — рост обхвата не «хорошо/плохо»), выбор поля → график во времени, форма замера (row-степперы, значения переносятся с прошлого раза), лог замеров
- [ ] F.3 Фото прогресса — позже (нужен Storage, план 02 отложенное)

### Этап G — Главный экран  ✅ 2026-09-04

- [x] G.1 Дашборд: карточка «Сегодня/Продолжить» + блок «Неделя» (тренировок · подходов за 7 дней + мини-бары топ-5 групп) + полоска «Последний рекорд» (тап → Прогресс). Вес тела не показываем — Григорий его не ведёт; вместо этого на плитке «Тело» счётчик дней с последнего замера, если ≥28
- [x] G.2 Плитки: Дневник · Прогресс · Тело — все живые. «Библиотека» отдельной плиткой не делаю пока (B.5), лист «+ упражнение» в сессии закрывает
- [x] G.3 Спокойно: один hue (accent), мини-бары 6px, без крупных чисел, всё тапабельно ведёт в раздел

_Итог G:_ главный экран показывает состояние (была ли неделя, где рост, что дальше) — закрыт исходный фидбек «главный экран пустой».

### Карта тела (по референсу «Корпус силовых», после G)

Из `context/04`: «Виджет "Что нагружено за неделю": карта тела с заливкой по объёму» — референс друга. Добавлено на экран «Прогресс»:

- `src/components/body-map.tsx` — стилизованная фигура спереди/сзади (SVG, не анатомический атлас), 16 зон = 16 `MuscleGroup`, заливка одним hue от объёма, тап по зоне → подпись с числом подходов
- Плашки сверху: подходов за период / групп в коридоре / тренировок
- Список «Меньше всего нагружено» — по возрастанию, с ориентиром
- **Ориентир пересчитан с потолка (8–16/4–10) на калибровку по факту программы v1**: крупные группы 6–14/нед, мелкие/вспомогательные 2–6/нед (см. HISTORY — посчитано по `program-v1.ts`, не по общему консенсусу, т.к. считаем только основную мышцу упражнения, без вторичных)

---

## Порядок и гейты

Не всё сразу. Предлагаемая последовательность (уточнена 2026-09-04 по ответам Григория):

1. **A.4–A.6** (свободная сессия дособрать) → Григорий проводит настоящую тренировку не по A/B/C
2. **B.1–B.2 + C** (библиотека-минимум + импорт истории) → главный экран и дневник наполняются реальными данными
3. **E** (прогресс) → появляется то, ради чего всё
4. **F** (тело) — есть исторические замеры, кладём их (см. `context/08`)
5. **D** (кардио) — сильно вниз: Григорий кардио почти не делает. В начале тренировки — растяжка/разминка (йога-подобная, раскрыть грудь). Это не кардио и не рабочий подход — максимум галка/заметка сессии, не отдельная модель. Бег/вело — «может, потом»
6. **B.3–B.5** (гифки) — Григорий: «пока необязательно». Хвост очереди
7. **G** (дашборд) — когда есть что показывать

Гейт после каждого этапа — «ОК» Григория.

## Ответы Григория (2026-09-04)

- **«Glute» в дневнике** = разведение ног в тренажёре (отведение бедра) → слаг `hip-abduction`. ⚠️ На C проверить записи, где `Glute` и `Hip abduction` стоят в один день (03.05, 17.05) — возможно, одна из них на самом деле сведение.
- **Замеры тела** — база от 5.12.25 (12 обхватов, бицепс раздельно L/R), +1–2 см везде к 22.01.26. Полные данные и вывод для модели — `context/08-telo-zamery.md`. Вес тела в замерах Григорий не вёл.
- **Кардио** — почти не делает, трекать как второстепенное. Растяжка в начале — отдельная история, не силовой подход.
- **Гифки** — необязательны сейчас.
