# Скопировать в .env и заполнить из Supabase → Settings → API.
# .env в гите не лежит. Те же значения — в Vercel → Project → Settings → Environment Variables.

VITE_SUPABASE_URL=https://xxxxxxxxxxxx.supabase.co
VITE_SUPABASE_ANON_KEY=eyJhbGciOi...
